apiVersion: v1
kind: PersistentVolume
metadata:
  name: dev-pv-traefik 
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/dev/traefik"
